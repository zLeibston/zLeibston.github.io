# zLeibston.World 博客指南

本项目是基于 [Hugo](https://gohugo.io/) + [PaperMod](https://github.com/adityatelange/hugo-PaperMod) 主题搭建的 GitHub Pages 博客。

---

## 📁 项目结构概览

```
├── config.toml          # 博客核心配置文件
├── content/             # 所有文章内容
│   ├── posts/           # 「随笔」系列
│   ├── rl-notes/        # 「RL自学笔记」系列
│   └── search.md        # 搜索页面
├── static/imgs/         # 静态图片资源
├── layouts/partials/    # 自定义模板（覆盖主题）
└── themes/PaperMod/     # 主题文件
```

---

## 🆕 一、新建一个系列（Section）

假设你要新建一个名为 `my-new-series` 的系列：

### 步骤 1：创建文件夹

在 `content/` 目录下新建文件夹：
```
content/my-new-series/
```

### 步骤 2：修改 `config.toml` - 添加到首页列表

找到 `mainSections` 配置项，将新系列加入数组：

```toml
[params]
  mainSections = ["posts", "rl-notes", "my-new-series"]  # 添加新系列
```

> ⚠️ **重要**：只有加在 `mainSections` 里的文件夹，文章才会显示在首页列表中！

### 步骤 3：修改 `config.toml` - 添加导航栏菜单

在 `[[menu.main]]` 部分添加新菜单项：

```toml
[[menu.main]]
  identifier = "my-new-series"
  name = "新系列名称"           # 导航栏显示的名称
  url = "/my-new-series/"      # 对应文件夹路径
  weight = 3                   # 排序权重，数字越小越靠前
```

### 步骤 4：（可选）为系列创建静态图片文件夹

如果该系列需要插入图片，建议在 `static/imgs/` 下创建对应文件夹：
```
static/imgs/imgs4my-new-series/
```

引用方式：`![描述](/imgs/imgs4my-new-series/xxx.png)`

---

## ✍️ 二、在现有系列下添加新文章

### 步骤 1：创建 Markdown 文件

在对应系列文件夹下创建 `.md` 文件，例如：
```
content/rl-notes/new_article.md
```

### 步骤 2：编写文章头部元信息（Front Matter）

每篇文章开头必须包含 YAML 格式的元信息：

```markdown
---
title: "文章标题"
date: 2026-02-08
draft: false
math: true                    # 是否启用数学公式（可选）
cover:
    image: "https://picsum.photos/800/400"  # 封面图片 URL
    alt: "封面描述"
    caption: "图片说明"
---

正文内容从这里开始...
```

### Front Matter 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | ✅ | 文章标题 |
| `date` | ✅ | 发布日期，格式 `YYYY-MM-DD` |
| `draft` | ✅ | `false` = 发布，`true` = 草稿（不显示） |
| `math` | ❌ | 是否启用 KaTeX 数学公式渲染 |
| `cover.image` | ❌ | 封面图片 URL |
| `cover.alt` | ❌ | 封面图片的 alt 文本 |
| `cover.caption` | ❌ | 封面图片的说明文字 |

### 封面图片建议

- **随机风景图**：`https://picsum.photos/800/400`
- **固定图片**：`https://picsum.photos/id/6/800/400`（id 控制具体图片）
- **本地图片**：`/imgs/imgs4xxx/your-image.png`

---

## 📐 三、数学公式使用

本博客已启用 KaTeX 数学公式支持：

- **行内公式**：使用 `$...$` 或 `\(...\)`
  ```markdown
  质能方程 $E = mc^2$ 是物理学基础。
  ```

- **块级公式**：使用 `$$...$$` 或 `\[...\]`
  ```markdown
  $$
  V_\pi(s) = \mathbb{E}_\pi [ G_t | S_t = s ]
  $$
  ```

> 需要在文章 Front Matter 中设置 `math: true` 才能启用。

---

## 🖼️ 四、插入图片

### 本地图片

1. 将图片放入 `static/imgs/` 对应子文件夹
2. 在文章中引用：
   ```markdown
   ![图片描述](/imgs/imgs4rl-note/example.png)
   ```

### 外部图片

直接使用完整 URL：
```markdown
![描述](https://example.com/image.png)
```

---

## 🚀 五、本地预览与发布

### 本地预览

```bash
hugo server -D
```
然后访问 `http://localhost:1313`

> `-D` 参数会同时显示 `draft: true` 的草稿文章

### 构建发布

```bash
hugo
```
生成的静态文件在 `public/` 目录，推送到 GitHub 即可自动部署。

---

## 📝 六、快速检查清单

### 新建系列时：
- [ ] 在 `content/` 下创建文件夹
- [ ] 在 `config.toml` 的 `mainSections` 中添加系列名
- [ ] 在 `config.toml` 的 `[[menu.main]]` 中添加导航菜单
- [ ] （可选）在 `static/imgs/` 下创建图片文件夹

### 新建文章时：
- [ ] 在对应系列文件夹下创建 `.md` 文件
- [ ] 填写完整的 Front Matter（title, date, draft）
- [ ] 如需数学公式，设置 `math: true`
- [ ] 如需封面图，配置 `cover` 字段

---

## 🔗 参考链接

- [Hugo 官方文档](https://gohugo.io/documentation/)
- [PaperMod 主题文档](https://github.com/adityatelange/hugo-PaperMod/wiki)
- [KaTeX 支持的符号](https://katex.org/docs/supported.html)
